http:
  routers:
    truenas:
      rule: "Host(`truenas.vr-worlds.eu`)"
      entryPoints:
        - web
      middlewares:
        - entra-auth@docker
      service: truenas-ext

  services:
    truenas-ext:
      loadBalancer:
        servers:
          - url: "http://192.168.178.134"